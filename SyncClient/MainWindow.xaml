<Window x:Class="SyncClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sync Client" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Выбор папки -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Папка для синхронизации:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="FolderPathTextBox" Width="500" Margin="0,0,10,0"/>
            <Button Content="Выбрать..." Click="SelectFolder_Click" Width="100"/>
        </StackPanel>

        <!-- Настройки сервера -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="URL сервера:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="ServerUrlTextBox" Text="http://localhost:5008" Width="300" Margin="0,0,10,0"/>
            <TextBlock Text="API Key:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="ApiKeyTextBox" Text="my-secret-api-key-12345" Width="200"/>
        </StackPanel>

        <!-- Лог событий -->
        <TextBox x:Name="LogTextBox" Grid.Row="2" Margin="0,0,0,10" 
         IsReadOnly="True" 
         TextWrapping="Wrap" 
         VerticalScrollBarVisibility="Auto" 
         FontFamily="Consolas" 
         FontSize="12"
         AcceptsReturn="True"/>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="StartButton" Content="Начать синхронизацию" Click="StartSync_Click" Width="150" Margin="0,0,10,0"/>
            <Button x:Name="StopButton" Content="Остановить" Click="StopSync_Click" Width="100" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>